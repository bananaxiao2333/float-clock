# Python
__pycache__/
*.py[cod]
.venv/
*.spec
/build
/dist

# Note: .python-version is meant to be committed -- the README explains why it has
# to be pinned to 3.12.7 (the last uv build bundling Tk 8.6; Tk 9.0 has a
# transparency regression on macOS)

# Everybody's own times should not go into the repository (the template lives in config.example.toml)
/config.toml

# Editors / system
.DS_Store
.idea/
.vscode/
*.swp
